# 更新日志

## 0.1.70 - 2026-09-05

- 业务错误率指标改为按托管账号隔离存储，避免共享源渠道的低样本错误被扩散到同渠道的其他托管账号。
- 业务错误率必须在连续两个指标窗口同时达到样本数和错误率阈值，避免单个 5 分钟窗口的 2/5、3/9 等瞬时波动直接停用账号。
- 托管渠道不再因共享的渠道级业务指标直接进入隔离，账号级调度由对应托管账号的独立业务指标决定；迁移时清理历史错误的共享托管指标。

## 0.1.69 - 2026-09-05

- 修复策略配置、映射、模型检测和来源信任变更绕过评估合并器、重复触发全量调度评估的问题，连续变更只保留最新一次评估请求。
- 发布或重启后不再直接沿用旧的主动探测失败计数和旧业务指标执行批量停用；普通失败必须先获得当前进程启动后的新证据，明确余额、认证、账号或分组失效仍可立即隔离。
- 慢但健康的托管账号改为保留调度资格并降至末档优先级，避免速度指标瞬时波动导致渠道被批量禁用；真实业务错误率原因单独展示，避免被“最近流式抽样成功”覆盖。

## 0.1.68 - 2026-09-05

- 修复数据源重新认证后仍短暂沿用旧成功扫描时间的问题；凭据更新会清空旧扫描基线，必须完成新一轮成功扫描后才能恢复调度。

## 0.1.67 - 2026-09-05

- 调度候选新增托管账号同步状态、数据源状态与成功扫描新鲜度闸门；远端账号不存在、分组删除、认证失败和长期扫描失败的账号不再继续参与调度或快速恢复探测。
- 托管渠道探测接入实时业务错误率确认；达到全局样本与错误率阈值，或主动探测明确返回账号/分组失效时，立即进入隔离状态，不再被 7 天历史成功率错误放行。
- 远端动作遇到账号不存在时自动将本地账号置为不可调度并标记同步失败；优先级和启停动作增加失败冷却，避免每个调度周期重复打爆远端接口。
- 数据源增加最近成功扫描时间，区分失败尝试和成功刷新；业务指标采集按请求 ID 去重，避免分页或接口重复记录放大错误率。

## 0.1.66 - 2026-09-04

- 新增模型能力闸门：新渠道接入后自动执行一次 7 项随机化能力检测，也支持管理员在“渠道雷达”按渠道手动触发；默认统一使用系统设置中的 `gpt-5.6-sol`。
- 模型能力检测记录分数、逐项任务结果、远端 Token 用量、触发来源和错误类型；技术故障与明确能力失败分开处理，避免网络或认证问题被误判为模型能力低。
- 新增人工强制通过与撤销操作，要求填写并保留审计原因；人工放行只绕过模型能力闸门，不绕过健康、余额、倍率、来源信任和人工暂停限制。
- 策略中的模型字段明确为业务测速模型，模型能力检测模型迁移到全局系统设置；检测不会因定时探测、映射变化、模型列表变化或普通告警自动重跑。
- 价格优先策略新增按最低源倍率动态计算目标分组倍率，支持固定增加或百分比上浮，并在影子模式、紧急冻结和目标写入失败时保留安全边界与事件记录。
- 增强渠道状态并发保护、目标节点同步串行化、命名空间模型识别和服务重启后的检测任务恢复处理。

## 0.1.65 - 2026-08-28

- 简化邮件提醒规则：数据源余额低于可配置阈值时发送，默认阈值为 10；目标分组没有可用托管账号时发送。
- 移除扫描、同步、动作、恢复和余额预测等邮件触发条件，其他情况只保留在事件中心展示。

## 0.1.64 - 2026-08-28

- 修复数据源会话保活写库参数未显式指定时间类型，避免启用或刷新时 PostgreSQL 报 `$4` 类型推断错误。

## 0.1.63 - 2026-08-28

- 新增独立数据源会话保活任务，Access Token 不再依赖托管账号是否参与调度或数据源扫描触发刷新。
- 按 JWT 到期时间提前刷新，无法读取到期时间的令牌使用固定保活周期；刷新失败采用指数退避并记录下次重试时间。
- 数据源认证锁改为按源独立，避免一个远端刷新阻塞其他数据源；控制台接口增加令牌到期、最近刷新和失败状态字段。

## 0.1.62 - 2026-08-25

- 修复分组策略异步动作乱序：同一托管账号的启停、优先级和兜底动作按账号串行执行，较新的动作会替代旧动作。
- 远端写入完成后增加最新动作校验，并将本地状态更新与动作完成状态放入同一事务，避免旧动作晚到后把可用渠道关闭或把不可用渠道重新打开。
- 远端启停状态校正跳过待审批和待执行动作，避免校正线程覆盖策略正在执行的结果。

## 0.1.61 - 2026-08-23

- 分组策略新增缓存观测与低缓存降权：从目标节点标准用量接口采集缓存读取、缓存创建和未缓存输入，按完整输入 Token 口径计算缓存读取比例。
- 缓存判断按模型、请求类型和独立 5 分钟窗口隔离；仅在自身样本达标且至少有 3 个同类对照账号时，使用排除自身后的中位数作为基准。
- 默认同时要求低于基准至少 10 个百分点和 25%，连续 3 个窗口才降权；恢复需连续 3 个新窗口且降权至少保持 15 分钟，数据不足或过期不会误解除已有降权。
- 新增缓存预热、观察、偏低和过期状态，并为低缓存账号提供每小时 5 分钟的轮换探测流量；策略支持关闭、只观测和自动降权三种模式。

## 0.1.60 - 2026-08-21

- 移除数据源稳定性中的“不稳定”“数据不足”和“有波动”徽标，仅保留稳定与人工不可信等需要明确提示的状态；真实成功率、首响和样本数指标继续展示。
- 调度运行和策略模拟不再显示无样本时的“数据不足”首响标签，保留 P50/P90 与样本数量供判断。

## 0.1.59 - 2026-08-21

- 数据源删除改为持久化异步任务：先逐个删除远端托管账号，再清理本地托管账号和数据源；失败任务保留错误信息并可从剩余账号重试，服务重启中断的任务会明确标记为失败。
- 删除中的数据源统一进入停用状态，禁止编辑、扫描、信任、调度和绑定等冲突操作；控制台即时展示删除进度，不再因远端清理耗时阻塞请求。
- 绑定任务的并发限制从整个数据源收窄到来源分组：同一来源分组在任意目标上的重复绑定仍被保护，其他来源分组可并行新增绑定，控制台也按分组独立展示忙碌状态。

## 0.1.58 - 2026-08-15

- 慢速兜底不再调用 Sub2API 的账号复制接口，改为读取旧账号当前模型映射、重新创建禁用态账号，完成优先级与调度切换后删除旧账号；模型映射保持原样。
- 重建中途失败时恢复旧账号并降到 900000+ 末档，切换成功但删除失败时持久化记录并每 15 分钟重试，避免留下持续参与调度的旧账号。
- 修复动作失败每 15 秒重复执行并产生独立告警的问题：失败重建冷却 15 分钟，同一账号同一动作只保留一条告警，远端状态校正同时避让重建动作。

## 0.1.57 - 2026-08-15

- 慢首响判定改用最近 5 分钟真实业务 P50/P90，P90 直接按策略上限判断；普通越线连续两批确认，达到上限 1.5 倍立即判慢，移除单次 60 秒样本直接封禁托管渠道的旧逻辑。
- 调度优先级拆分为正常、观察、慢速兜底三个区间，分别从策略起点、300000、900000 开始，保留至少 1000 的档位间隔。
- 常规可用渠道达到保底数量时，慢渠道直接退出；不足时停用并保留旧远端账号，再复制为新的末档兜底身份，避免粘性请求继续命中旧慢账号。
- 新增远端账号替换历史和失败回滚；替换过程只同步启停与优先级，不修改模型映射，界面同步展示 P50、P90、判定状态和兜底身份。

## 0.1.56 - 2026-08-14

- 调度策略新增可配置的首 Token 上限，默认 10 秒；超限渠道不再仅排到队尾，策略模拟和调度运行页面同步展示首响、门槛与判定结果。
- 新增分组容量保护：实际参与调度的正常渠道不足 5 个时，仅选择最快的慢渠道低优先级补足；达到 5 个后慢渠道自动退出调度。
- 慢速兜底采用“停用、降低优先级、恢复”的串行动作，失败时不会提前放回流量；正常在线渠道调整档位不会停用，避免整组抖动。
- 策略开放保底渠道数、优先级起点和优先级间隔配置，默认从 1000 起、每档相差 1000，间隔最低为 1000。

## 0.1.55 - 2026-08-10

- 渠道雷达为异常渠道新增“立即快速验证恢复”操作，可绕过慢首响隔离的最长 24 小时等待；慢首响渠道立即执行最多 3 次连续恢复抽样，通过后马上重新评估并恢复调度。
- 手动快速验证增加同渠道并发保护，人工暂停渠道必须先恢复后验证；普通异常渠道执行一次即时验证，并记录验证开始、结果和最终状态审计。

## 0.1.54 - 2026-08-07

- 移动端数据源、渠道、托管账号、调度记录、审计记录和策略模拟表格改为记录式布局，移除窄屏固定列遮挡，长名称、地址和状态原因可完整查看。
- 数据源绑定主操作在移动端独占一行，图标操作扩大触控区域；筛选、策略、目标节点和设置页面同步优化窄屏排列。
- 绑定弹窗改为单层滚动，来源分组、目标分组、映射预览和保存操作不再互相遮挡；平板竖屏沿用移动布局并在桌面宽度恢复表格。

## 0.1.53 - 2026-08-07

- 数据源列表新增明确的“开始绑定 / 管理绑定 / 查看绑定”操作入口，分组绑定不再依赖点击数据源名称；绑定弹窗同时突出当前数据源和分组绑定语义。
- 修复创建托管账号时未传递源分组倍率、导致目标节点账号倍率默认为 `1` 的问题；批量绑定、手工创建、绑定回滚和平台重建统一同步正确倍率。
- 目标节点同步新增存量托管账号倍率校正，源分组倍率刷新后立即复检；校正失败会进入事件中心并沿用账号配置校正邮件开关。
- 托管账号列表新增账号倍率与渠道倍率对照，方便直接识别尚未校正的倍率漂移。

## 0.1.52 - 2026-08-06

- 数据源支持使用 `http://公网 IP:端口` 直连；域名、目标节点和发布地址仍默认要求 HTTPS，内网、回环和共享地址空间继续拒绝。

## 0.1.51 - 2026-08-06

- 首响 P95 优先使用真实业务指标；源站未提供真实首响时回退到最近 7 天主动探测 P95，并在详情中明确标注指标来源。
- 探测首响至少积累 20 个样本后才参与稳定性评估，避免少量样本造成误判。

## 0.1.50 - 2026-08-06

- 数据源质量参考改为纯稳定性评估，仅输出稳定、有波动、不稳定或数据不足，不再生成是否充值的建议。
- 稳定性评估去除倍率空间等经营因素，优先使用 7 天真实业务成功率、真实首响 P95 和扫描中断次数；主动探测仅在真实业务样本不足时参与定级。
- 数据源列表直接展示真实成功率、首响和请求样本；详情页新增四项原始指标，评估结论可直接核对。
- 余额提醒邮件移除站点质量与充值意愿判断，继续只保留余额、最低充值额、耗尽时间、账号和充值地址。

## 0.1.49 - 2026-08-06

- Sub2API 余额采集兼容 `credit_balance` 与驼峰字段；账户资料接口失败或缺少余额字段时不再静默显示扫描成功，避免余额长期未知。
- 数据源新增独立的暂停调度与恢复调度按钮；暂停后立即停用该站全部托管账号并阻止自动恢复，数据扫描、余额预测和提醒继续运行。
- 质量参考改为真实业务优先：足量业务数据不再被测试模型不兼容直接判为停止充值，探测与业务明显冲突时改为提示检查测试模型。
- 已有绑定缺少生效策略时，在数据源详情中明确列出目标分组并说明不会自动调度。

## 0.1.48 - 2026-08-04

- 邮件场景“账号平台校正失败”扩展为“账号配置校正失败”，统一控制账号平台格式与模型映射校正失败通知。
- 未映射到设置开关的事件默认不再发送邮件，避免新增事件绕过用户选择；事件仍保留在事件中心。
- 故障恢复邮件改为默认关闭，继续保留独立开关供需要恢复确认的用户手动开启。

## 0.1.47 - 2026-08-04

- 分组策略的测试模型改为可自由填写的文本框，并按目标分组和平台自动带入一个可覆盖的默认模型。
- 分组禁用模型改为多行文本框，每行填写一个模型名称，保存时自动去除空行和重复项。
- 后端取消测试模型和禁用模型必须存在于目标节点同步清单中的限制，支持直接配置尚未被节点枚举的新模型；继续阻止测试模型被同时禁用。

## 0.1.46 - 2026-08-04

- 数据源新增人工“不可信”标记；人工结论是唯一会影响业务的质量设置，生效后保留历史数据，同时退出自动调度、停止余额提醒并禁止新增或修改托管绑定。
- 系统根据业务成功率、主动探测、首 Token 延迟、扫描异常和倍率空间生成质量建议、置信度及最多三条理由，结论仅供参考，不会自动改变调度或提醒。
- 余额邮件精简为当前余额、建议充值、预计耗尽、充值账号和充值地址；仅在系统建议停止充值时附加一行参考提示，恢复邮件只展示余额与恢复时间。
- 数据源新增独立充值地址配置；取消不可信标记后先重新验证渠道，再恢复参与调度。

## 0.1.45 - 2026-08-03

- 分组策略新增禁用模型清单；启用后从该目标分组全部托管账号的模型映射中移除禁用项，停用或删除策略时自动恢复完整映射。
- 批量绑定、手工创建、绑定回滚和平台重建统一应用禁用清单，避免后续维护重新开放已禁用模型；过滤后没有可用模型的账号会保持停止调度。
- 策略新建、编辑和列表页面增加禁用模型选择与生效清单，并阻止测试模型与禁用模型冲突。

## 0.1.44 - 2026-08-02

- 速度优先策略改为优先使用每个托管账号最近 10 次真实业务首 Token 中位数，数据缺失或过期时回退主动探测，并在渠道雷达和调度视图标明指标来源。
- 修复 Anthropic / Kiro 与 Gemini 托管账号被错误追加 `/v1`、目标节点再次拼接接口路径后无法访问的问题；新建、手工创建和目标同步统一按平台生成上游地址，既有账号会自动校正一次。
- README 补充项目用途、使用收益、适用人群和周边服务入口。

## 0.1.43 - 2026-07-30

- 数据源绑定页面改为按单个源分组编辑：选择源分组后自动回显当前目标分组，可直接勾选新增或取消移除并保存最终绑定。
- 绑定更新按远端差异执行并在数据库事务中一次提交；失败时清理新账号、恢复已删除账号及原调度状态，避免本地与目标节点关联不一致。
- 移除绑定时同步作废旧账号尚未执行的调度动作；新增账号继续严格使用目标分组平台对应的模型族。

## 0.1.42 - 2026-07-29

- 删除数据源时同步关闭其扫描失败和余额事件，避免事件中心残留已不存在对象的告警。

## 0.1.41 - 2026-07-29

- 托管渠道普通抽样失败保持在线的同时保留失败计数，使恢复探测可按 15 秒、60 秒、300 秒逐级退避，避免持续请求异常上游。

## 0.1.40 - 2026-07-29

- 托管渠道的普通主动抽样失败仅记录结果，不再累计失败次数或触发隔离。
- 明确区分托管与非托管渠道的失败状态更新，保留首 Token 超过 60 秒的慢通道隔离。

## 0.1.39 - 2026-07-29

- 托管账号模型映射严格跟随目标分组平台：GPT 仅保留 `gpt-*`、Claude 仅保留 `claude-*`、Gemini 仅保留 `gemini-*`、Grok 仅保留 `grok-*`；目标同步会自动校正既有账号，新增账号从创建时即执行同样限制。
- 测试模型从系统设置迁移到分组策略版本，新建、编辑和策略卡片直接展示该策略使用的测试模型；主动探测只使用已启用策略配置的模型。
- 每个托管账号继续强制只绑定一个目标分组，避免跨平台分组共享账号导致模型族混用。

## 0.1.38 - 2026-07-29

- 托管渠道的普通主动探测失败必须达到连续确认次数才会隔离，不再因单次失败叠加历史业务指标而立即停用；首 Token 超过 60 秒仍立即退出调度。

## 0.1.37 - 2026-07-29

- 分组测试模型不在某个既有源渠道的模型列表时跳过该渠道的流式抽样，不再把模型配置不匹配累计成渠道故障或隔离；目标分组设置和现有模型映射保持不变。

## 0.1.36 - 2026-07-29

- 每个目标分组新增可编辑的测试模型，探测只使用分组指定的同平台普通文本模型；GPT 分组不再误用 DeepSeek、审查、图片、音频或实时模型。
- 同一数据源的渠道改为串行探测，避免集中抽样触发上游并发限制；普通失败达到确认次数前保持现有调度，只有慢首响和确认故障才停用。

## 0.1.35 - 2026-07-29

- 首响抽样排除 `codex-auto-review`、嵌入、音频、实时、图片等非普通文本生成模型，确保速度判定代表实际生产文本请求。

## 0.1.34 - 2026-07-29

- 速度优先策略改为使用专用 Key 的轻量流式生成抽样首 Token P95，不再把 `/v1/models` 模型列表响应时间当作生成首响；没有真实首响样本的渠道排在有样本渠道之后。
- 任意一次真实业务首 Token 超过 60 秒的渠道立即隔离并退出所有自动调度；系统使用轻量流式生成抽样恢复，失败后按 1 分钟、5 分钟、30 分钟、2 小时、6 小时、24 小时逐级退避，连续 3 次合格才恢复参与。
- 渠道雷达与市场大盘的首 Token P95 同步改为真实流式抽样数据，调度原因明确展示慢首响数值和 60 秒上限。

## 0.1.32 - 2026-07-29

- 修复 Sub2API 用户专属分组倍率被分组默认倍率覆盖的问题；调度、市场大盘和绑定页面统一按“用户专属倍率优先，未配置时回退默认倍率”计算，并继续应用数据源余额/倍率换算比例。

## 0.1.31 - 2026-07-29

- 数据源分组创建改为持久化后台任务：点击确认后立即返回，页面显示逐组进度并允许关闭；完成后只刷新当前数据源，不再打断列表分页和连续绑定操作。
- 同一数据源仅允许一个创建任务，防止重复点击生成重复账号；服务重启会明确终止遗留任务，失败批次继续自动回滚远端 Key 和托管账号。
- Sub2API 返回滑块验证或登录会话上限时，数据源进入“需要重新认证”并停止定时自动重登；数据源编辑支持更新账号密码或 Access Token + Refresh Token 后重新扫描。

## 0.1.30 - 2026-07-29

- 修复托管账号本地显示停止但 Sub2API 远端仍在运行的问题；新建账号后显式同步停止状态，并在目标同步时仅核对本系统托管账号，自动纠正远端启停漂移。
- 分组策略新增“允许等倍率账号参与”开关，默认关闭；关闭时源分组倍率必须严格低于目标分组倍率，高于或相等的账号都会退出调度。
- 策略卡片、模拟结果和调度原因明确展示倍率越界或等倍率未获允许，便于核对账号为何参与或停止。

## 0.1.29 - 2026-07-29

- 余额告警邮件标题直接显示数据源名称和当前余额；预测不足时同时显示预计剩余时间，无需打开邮件即可判断需要充值的账号和紧急程度。
- 余额恢复邮件标题直接标明对应数据源已恢复，并避免展示告警发生时的过期余额。

## 0.1.28 - 2026-07-29

- 托管账号类型改为严格跟随目标分组平台，不再沿用源分组类型；自动校正历史错误账号，Grok、CCMAX 等分组会创建对应平台格式的独立账号，并在安全切换后清理旧账号。
- 自动调度优先级改为从 1000 起每档相差 100；分组无可用账号时持续快速复检，余额恢复、渠道恢复或停止后重新可用的账号会自动加入调度。
- 新增按实际余额消耗中位数计算的续航预测，区分工作时段、夜间和周末预警提前量；余额为零或源站明确返回余额不足时立即 P0，预测告警和普通恢复需连续两次扫描确认，降低短时峰值导致的误报。
- 余额邮件增加源站地址、充值账号、当前余额、消耗速度、预计剩余、预计耗尽时间和建议最低充值金额；所有生产邮件统一说明发生事项、影响、处理动作和系统自动恢复流程。
- 系统设置新增余额不足、数据源扫描、目标同步、分组无人可调度、远程写入、账号平台校正和恢复通知等邮件场景开关；单渠道短时探测失败仅在页面展示，不再发送模糊的“渠道探测异常”。
- 邮件发送增加 Resend 幂等键、临时故障退避重试和响应校验，避免重复投递，并对 API Key、发件域名等永久配置错误给出明确提示。
- 数据源列表与运营总览增加余额消耗速度和预计可用时长；托管账号和映射预览展示实际目标平台，便于核对账号格式与目标分组是否一致。

## 0.1.27 - 2026-07-29

- 调度运行支持手动隐藏目标分组；隐藏状态保存在当前浏览器，刷新或下次打开仍生效，并可在已隐藏分组区域逐个或全部恢复。隐藏仅影响页面展示，不改变账号和实际调度状态。
- 市场 30 天倍率折线的右侧端点标签改为紧邻展示“分组名 + 当前倍率”，缩短名称与数值之间的距离，便于直接对应每条曲线。

## 0.1.26 - 2026-07-29

- 调度运行改为按目标分组展示实际参与队列，账号按 `1000` 起的远端优先级排序；未参与账号默认隐藏，可按分组展开查看策略阻断原因、样本进度和预计恢复时间。
- 新绑定、停止后等待恢复以及分组无可用账号时启用独立快速恢复循环，最快每 15 秒并发复检；最近连续 3 次成功即可恢复调度，持续失败自动退避到 1 分钟和 5 分钟，正常参与账号仍按常规周期巡检。
- 修复历史调度动作永久阻止同一账号再次停止或恢复的问题；调度动作现在只去重尚未完成的重复写入，支持账号多次故障和恢复。
- 市场 30 天倍率折线右侧同时标注目标分组名称与当前倍率值，长名称截断时仍保留完整倍率，便于直接判断每条曲线的最新水平。

## 0.1.25 - 2026-07-29

- 市场倍率折线的分组名称统一放入图表右侧独立标签区，自动上下错开，并通过同色引导线连接对应曲线，解决多分组标签重叠、无法辨认的问题。
- 调度运行新增账号级当前状态视图，可按目标分组和参与状态筛选并分页，直接展示来源、倍率、7 天样本、实际参与状态、策略判定原因和预计样本积累时间。
- 策略模拟改为弹窗展示每个托管账号的判定结果；调度执行记录补充账号、来源、目标分组、启停或优先级变更及远端同步结果，立即评估后会明确刷新反馈。

## 0.1.24 - 2026-07-29

- Sub2API 自动绑定会读取源站发布的独立 API 地址，修复 Vincent 等面板域名不承载 `/v1` 接口时的绑定回滚；错误信息会直接说明发布 API 地址异常。
- 自动调度的样本与成功率窗口统一改为 7 天，解决 15 分钟探测周期下无法达到策略最少样本数、所有托管账号长期停止调度的问题。
- 数据源列表新增已绑定源分组数和托管账号数，支持按绑定状态、扫描状态筛选，以及按账号数、余额、分组数排序；绑定成功后保留当前分页、筛选条件和绑定窗口，可连续处理其他分组。
- 运营总览新增源分组覆盖率、待处理数据源和余额高低榜单；市场倍率折线在线尾直接标注目标节点和分组，并自动避让相邻标签。

## 0.1.23 - 2026-07-29

- 策略卡片明确展示自动调度状态、参与账号数和 30 秒评估周期，支持直接停用策略；移除实际未生效的确认次数和冷却时间配置。
- 修复中文数据源或分组导致自动生成的 New API Token 名称超过 50 字节限制的问题；保留唯一后缀，并直接展示名称或余额等源站业务错误，不再误报网关中断。
- 数据源详情改为立即打开，目标分组独立加载；倍率缓存过期时先返回已有数据并去重后台刷新，只有缓存真正缺失时才同步等待源站。

## 0.1.22 - 2026-07-29

- 市场大盘的低分渠道和稳定渠道榜单增加分页，支持每页 15、30、50 条；翻页排名连续，切换分组或榜单时自动回到第一页。
- 策略卡片增加编辑和删除入口；编辑会保留目标分组并生成新版本，删除策略及其历史版本但不影响渠道、托管账号或远端数据。
- 修复双击创建生成两条分组策略的问题：提交期间锁定按钮，后端按目标分组串行校验并禁止重复策略；已有策略的分组在新建时直接禁选。

## 0.1.21 - 2026-07-29

- 在线更新下载安装后持久记录待重启版本，页面被刷新或重新登录时仍会恢复“立即重启”入口。
- 新版本启动后自动清理已生效标记；执行版本回滚时同步清理未生效标记，避免显示错误的更新状态。

## 0.1.20 - 2026-07-29

- 系统设置新增网页在线更新中心，可检查 GitHub 正式版本、下载安装包并在页面内重启生效。
- 更新包强制校验 SHA-256 和可信下载域名，替换前自动保留当前程序，支持从网页回滚上一版本。
- Docker 正式镜像改用应用用户可写的独立程序目录，为后续版本提供无需 SSH 的原地更新能力。

## 0.1.19 - 2026-07-29

- 市场大盘只统计已经绑定到目标分组的托管渠道，不再展示与本系统无关的源站全量分组。
- 倍率趋势按目标分组和小时聚合，支持平均值、中位数、最低值切换；总览展示所有目标分组折线，也可切换到单个分组。
- 趋势下方增加低分渠道与稳定渠道 Tab，展示质量分、倍率、7 天主动探测和真实业务样本，并随总览或具体分组范围联动。
- 自动绑定回滚改为后台清理，避免远端删除耗时吞掉真实错误；源站余额不足时明确显示具体源分组和余额原因。

## 0.1.18 - 2026-07-29

- 数据源、市场、目标节点、渠道雷达、托管账号、策略、调度记录、事件和审计列表统一增加分页，支持每页 15、30 或 50 条；搜索、刷新和切换页面时自动回到第一页。
- 一个源分组绑定多个目标分组时，远端账号改为最多 6 路受控并发创建，降低串行写入触发网关 502 超时的概率；批次失败仍完整回滚已创建账号和上游 Key。
- 自动绑定失败写入服务端日志；网关直接中断且没有返回 API 错误正文时，页面显示可执行的回滚和重试提示。

## 0.1.17 - 2026-07-29

- 新建渠道 Key 和托管账号的默认并发改为 1000，映射页面默认显示 1000。
- 托管账号列表支持修改并发，保存后立即同步目标 Sub2API；手工修改优先级和并发不再被影子模式拦截。

## 0.1.16 - 2026-07-29

- 旧全局策略按同名目标分组自动迁移；无法唯一匹配的策略自动停用，避免显示为启用却不参与调度。
- 数据源扫描失败后按配置周期重试，不再每 30 秒重复登录并持续触发 429 或会话上限。

## 0.1.15 - 2026-07-29

- 渠道雷达不再把自动生成的分组专用 Key 横向关联到其他源分组；升级时清理没有托管账号引用的历史错误关联，消除重复渠道。
- 新建托管账号默认优先级改为 1000；托管账号列表支持修改优先级，并立即同步到目标 Sub2API。
- 策略改为按目标分组独立配置，支持价格优先和速度优先，自动从 1000 开始重排该分组下符合条件的托管账号。
- 策略写动作无需人工确认；影子模式或紧急冻结时直接跳过，不再产生待确认动作，升级时自动关闭历史待确认项。

## 0.1.14 - 2026-07-29

- 策略倍率上限改为托管账号所属目标分组的自动倍率，禁止前端和 API 人工配置；倍率缓存 3 分钟，缺失或过期时自动从目标节点刷新。
- 策略模拟与自动调度按每个独立账号比较“源分组倍率 <= 目标分组倍率”，目标倍率缺失时停止评估，禁止默认放行。
- 目标分组选择列表同步展示目标节点倍率；通用托管账号接口强制每个账号只绑定一个目标分组。
- 移除旧的“同一渠道在同一目标节点只能有一个账号”数据库约束，支持一个源分组按多个目标分组分别创建和调度账号。

## 0.1.13 - 2026-07-29

- 修正源分组到目标分组的部署语义：按“源分组 × 目标分组”创建独立托管账号，每个账号只绑定一个目标分组并接受该分组策略的单独调度。
- 映射预览改为逐账号展示，并明确显示最终账号总数；批量创建请求按实际账号数量放宽超时。
- 远端创建失败时逐个回滚本批次已经创建的托管账号和上游 Key，避免残留半成品。

## 0.1.12 - 2026-07-29

- 应用重启时自动复位中断后遗留的 `RUNNING` 数据源扫描，避免该数据源永久跳过后续调度和手动重扫。

## 0.1.11 - 2026-07-29

- 修复 Sub2API 分组倍率采集：优先读取分组接口的 `rate_multiplier`，并兼容旧版独立倍率映射。
- 源分组选择列表和一对多映射预览明确展示“倍率 ×N”；远端未提供时直接标识“倍率未提供”。

## 0.1.10 - 2026-07-29

- 升级时自动关闭历史已确认事件，以及已恢复在线或已删除目标节点遗留的同步事件。
- 事件中心完整展示当前活动故障，但最近恢复记录最多保留 10 条，避免历史事件淹没当前状态。

## 0.1.9 - 2026-07-29

- 目标同步不再拉取或镜像节点上的全量账号，仅同步版本和目标分组；账号管理严格限定为渠道管家自己创建并记录所有权的托管账号。
- 升级时删除历史 `protected_accounts` 镜像数据和无用的分组保护字段，目标节点接口同步移除该查询能力。

## 0.1.8 - 2026-07-29

- 目标节点账号列表改为小页分页同步，修复大型 Sub2API 实例单页响应超过 8 MiB 后被截断并误报非 JSON 的问题。
- 远端响应超过读取上限时返回明确的容量错误，避免与协议格式变化混淆。

## 0.1.7 - 2026-07-29

- Sub2API 管理接口自动携带管理端请求标识，修复目标节点返回网页内容并被误报为 `SCHEMA_CHANGED` 的问题。
- 远端响应按 HTTP 状态和内容类型准确分类；仅在明确 `404` 时回退旧版认证路由，限流、鉴权失败和 HTML/WAF 响应保留具体接口与原因。
- 数据源分组映射改为三步式一对多界面，提交前逐行展示“源分组 -> 多个目标分组”的最终关系，并优化桌面端和移动端布局。
- 扫描、同步、探测类事件改为状态事件：同一对象自动去重和更新，恢复后自动关闭，事件中心不再要求逐条人工确认。
- 升级时自动合并历史重复活动事件；事件中心仅保留活动事件及最近 24 小时的恢复记录。

## 0.1.6 - 2026-07-28

- 远端认证改为持久化会话：首次登录后加密保存 Access Token、Refresh Token、Cookie 和会话标识，后台任务不再周期性重复调用登录接口。
- Sub2API 同时兼容“Access Token + Refresh Token”和“仅 Access Token”；有 RT 时自动轮换，无 RT 时仅在 Access Token 失效后重新登录。
- New API 同时兼容新版 Access Token + HttpOnly 刷新 Cookie 会话和旧版登录 Cookie，会话失效后按远端能力刷新或重新登录。
- 登录接口遇到 429 时按 `Retry-After` 退避重试，限流不会误触发旧版路由回退，也不会因单次失败将目标节点判定为离线。

## 0.1.5 - 2026-07-28

- 新增渠道管家品牌 Logo，并同步更新登录页、桌面端、移动端和浏览器图标。
- 兼容 `lcodex.cc` 等直接返回数组或对象的新版 Sub2API 接口，不再误报 `SCHEMA_CHANGED`。
- 数据源扫描错误保留远端错误代码与具体信息，便于识别 `AUTH_SESSION_LIMIT` 等账号限制。
- 目标节点卡片直接展示同步失败原因，支持更新管理员凭据，并让手动同步即时返回结果。

## 0.1.4 - 2026-07-28

- 数据源详情改为展示远端分组、倍率和描述，支持直接选择源分组、目标节点及目标分组完成映射。
- 自动为每个源分组创建专用上游 Key 和目标 Sub2API 托管账号，并在失败时回滚已创建的远端资源。
- Sub2API 新增 Access Token + Refresh Token 认证，令牌失效后自动刷新并加密保存轮换后的令牌。
- 兼容 `lcodex.cc` 等使用 `/auth/login`、`/auth/refresh` 扁平认证路由的 Sub2API 部署。
- 兼容新版 New API 的 Access Token、会话标识和刷新 Cookie，并透传远端错误代码。
- 优化分组映射在桌面和手机端的操作布局，托管账号统一从数据源分组入口创建。

## 0.1.3 - 2026-07-28

- 优化管理端信息层级、文字与状态色对比度，提升桌面、平板及手机端可读性。
- 列表搜索改为按页面呈现，移动端增强表格扫描、粘性列和触控操作体验。
- 应用版本统一从运行实例读取，移除旧版本回退和流程虚假完成状态。
- 完善模态框键盘操作、焦点管理、消息播报及导航可访问性。

## 0.1.2 - 2026-07-28

- 数据源统一为生产资产，移除资产模式字段与界面选项。
- 新增按数据源配置的余额/倍率换算比例，支持 `1:10` 等口径。
- 修改换算比例时同步重算当前余额、当前倍率和历史样本，并立即重新扫描。
- 管理端配色、导航、图标、表格、表单及登录页视觉与 `account_manage` 统一。

## 0.1.1 - 2026-07-28

- 新增管理员登录邮箱与密码修改入口，要求验证当前密码。
- 修改账号后保留当前会话，并自动注销该账号的其他登录会话。
- 账号变更写入审计日志，补充邮箱格式、密码长度与邮箱冲突校验。

## 0.1.0 - 2026-07-28

- 将 Sub2API 上游运营控制面重构为 Go 单二进制与嵌入式 Vue 管理端。
- 支持 Sub2API、New API 数据源登录、分组倍率、余额采集和生产 Key 授权。
- 支持目标 Sub2API 分组及受保护账号镜像，只对带所有权标记的托管账号执行写操作。
- 支持主动探测与目标真实业务日志联合判定、渠道状态机、市场大盘、策略版本和影子评估。
- 支持动作审批、Resend 事件邮件、投递记录、事件中心与追加式审计。
- 默认开启影子模式，提供紧急冻结、目标写权限和托管账号默认停止调度等安全闸门。
- 提供 PostgreSQL 双容器部署、一键安装、升级前备份、跨架构二进制和 GHCR 镜像发布。
